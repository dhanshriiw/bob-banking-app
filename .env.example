# Environment variables — copy this file to .env and fill in real values
# Never commit the actual .env file to source control.

# Flask secret key — use a random 32+ character string in production
SECRET_KEY=dev-secret-key-change-in-production-32chars!!

# Optional: override the SQLite database path
# DB_PATH=BACKEND/db/banking.db
